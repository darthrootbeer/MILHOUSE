---
id: A1B2C3
status: pending
deps: []
files: [scripts/milhouse.sh]
---
::context
Create initial script structure with proper shebang, error handling, and mode detection (--once, --loop, --setup or defaults)

::done-when
- scripts/milhouse.sh exists with executable permissions
- Has #!/bin/bash shebang
- Has set -euo pipefail for error handling
- Can detect --once, --loop, --setup modes or default to loop
- Basic argument parsing works

::steps
1. Create scripts/milhouse.sh with shebang and error flags
2. Add argument parsing for modes (--once, --loop, --setup)
3. Add help text for --help flag
4. Add workspace detection (current dir or argument)
5. Make script executable: chmod +x scripts/milhouse.sh

::avoid
- Don't implement actual functionality yet, just skeleton
- Don't add gum UI yet (Phase 4)
- Don't add rotation/gutter logic yet

::notes
This is the foundation - keep it simple and clean. Reference ralph-wiggum-cursor scripts for structure inspiration but keep it single-file.
