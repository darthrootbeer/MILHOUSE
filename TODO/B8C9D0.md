---
id: B8C9D0
status: pending
deps: [Y5Z6A7]
files: [scripts/milhouse.sh]
---
::context
Add --once mode: run exactly one iteration then stop. Useful for testing before running full loop.

::done-when
- --once flag triggers single iteration mode
- Runs one iteration: build prompt, run agent, check completion
- Stops after one iteration regardless of completion status
- Reports results: completed or criteria remaining
- Returns appropriate exit code

::steps
1. In argument parsing, detect --once flag
2. Create run_single_iteration(workspace, model) function
3. Run: build_prompt(1), run_agent_iteration(), check_completion()
4. Report results: show completion status and remaining criteria
5. Return 0 if complete, 1 if incomplete (or always 0, just informational)

::avoid
- Don't run full loop (only one iteration)
- Don't check rotation (only one iteration)
- Don't check gutter (Phase 3)

::notes
--once is like ralph-once.sh - test one iteration before committing to full loop. Useful for debugging prompts and task setup. Reference ralph-once.sh for flow.
