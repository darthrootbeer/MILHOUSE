---
id: C5D6E7
status: pending
deps: [D4E5F6]
files: [scripts/milhouse.sh]
---
::context
Implement task file change detection: compare MILHOUSE_TASK.md hash between iterations, detect gutter if unchanged for 2+ iterations (no progress).

::done-when
- Function check_task_file_stale(iteration) detects if task file unchanged
- Stores task file hash in .milhouse/.task_hash or similar
- Compares hash from previous iteration
- Returns true if hash unchanged for 2+ iterations

::steps
1. Before iteration: calculate hash of MILHOUSE_TASK.md (md5 or sha256)
2. Store in .milhouse/.task_hash.{iteration}
3. After iteration: calculate new hash
4. Compare: if same as last 2 iterations, return GUTTER
5. Add check to main loop: if gutter detected, stop

::avoid
- Don't store full file content (just hash)
- Don't check on first iteration (need baseline)
- Keep hash storage simple (one file per iteration or track in state)

::notes
No progress detection: if task file unchanged for 2 iterations, agent isn't making progress. Uses file hash to detect changes. Simple but effective.
