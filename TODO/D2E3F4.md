---
id: D2E3F4
status: done
deps: [S9T0U1, O7P8Q9]
files: [scripts/milhouse.sh]
---
::context
Add status indicators: use gum spin for long operations (agent running) per GUM_TUI_RULES.md. Shows spinner during agent execution.

::done-when
- Function show_spinner(message) shows gum spin during agent execution
- Displays spinner with message: "Agent working..."
- Runs in background while agent executes
- Stops spinner when agent completes
- Falls back to simple echo if gum not available

::steps
1. Before agent run: start gum spin --spinner dot --title "Agent working..."
2. Run agent in background or with spinner
3. Wait for agent completion
4. Stop spinner when done
5. If no gum: use simple echo "Agent working..." (no spinner)

::avoid
- Don't block on spinner (run in background or use gum's built-in wait)
- Don't require gum (always have fallback)

::notes
Spinners show progress for long operations. gum spin handles this elegantly. If no gum, simple text message is fine. Follow GUM_TUI_RULES.md: use spinners for operations >2 seconds.
