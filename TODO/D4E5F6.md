---
id: D4E5F6
status: pending
deps: [A1B2C3]
files: [scripts/milhouse.sh]
---
::context
Read MILHOUSE_TASK.md file and parse checkbox format to extract task criteria. Task file uses markdown checkboxes: - [ ] or - [x]

::done-when
- Function read_task_file() can read MILHOUSE_TASK.md
- Can extract all checkbox lines (both [ ] and [x])
- Can count total checkboxes and completed ones
- Handles missing task file gracefully with error message

::steps
1. Add function read_task_file(workspace) that reads MILHOUSE_TASK.md
2. Use grep to find checkbox lines: grep -E '^[[:space:]]*[-*][[:space:]]+\[[x ]\]'
3. Count total checkboxes and completed ([x]) ones
4. Return structure with total, completed, remaining counts
5. Add error handling if file doesn't exist

::avoid
- Don't parse task content beyond checkboxes yet
- Don't implement task modification (that's agent's job)
- Don't use complex parsing - simple grep is enough

::notes
Checkbox format matches Ralph's: - [ ] or - [x] or * [ ] etc. Only count actual list items, not [ ] in prose. Reference ralph-common.sh check_task_complete() for regex pattern.
