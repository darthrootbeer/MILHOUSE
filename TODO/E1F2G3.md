---
id: E1F2G3
status: pending
deps: [Y5Z6A7]
files: [scripts/milhouse.sh]
---
::context
Add --loop mode (default): run continuous loop until completion or max iterations. This is the main mode.

::done-when
- --loop flag (or no flag) triggers loop mode
- Calls run_milhouse_loop() with max iterations
- Default max iterations: 20
- Can override with --max-iterations N flag
- Handles loop completion and reports results

::steps
1. In argument parsing, --loop is default (or explicit flag)
2. Parse --max-iterations N if provided, default 20
3. Call run_milhouse_loop(workspace, max_iterations, model)
4. Handle return code: 0 = success, 1 = max iterations reached
5. Report final status to user

::avoid
- Don't modify loop logic here (that's in run_milhouse_loop)
- Keep argument parsing simple
- Default behavior should be loop mode

::notes
--loop is the main mode. Default behavior when no flags. Just needs to call the loop function with right parameters. Max iterations prevents infinite loops.
