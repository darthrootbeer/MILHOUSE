---
id: F8G9H0
status: pending
deps: [K7L8M9]
files: [scripts/milhouse.sh]
---
::context
Implement time limit detection: if iteration runs >1 hour, trigger gutter (agent stuck or taking too long).

::done-when
- Function check_time_limit(duration) returns true if >3600 seconds (1 hour)
- Uses same time tracking as rotation heuristic
- Returns GUTTER if duration exceeds limit
- Integration: check after iteration, stop if gutter

::steps
1. Reuse time tracking from rotation heuristic (duration calculation)
2. Add check_time_limit(duration): return true if duration > 3600
3. This is different from rotation (rotation continues, gutter stops)
4. Add to main loop: check after iteration, stop if gutter
5. Log: "Gutter detected: iteration exceeded 1 hour limit"

::avoid
- Don't confuse with rotation (gutter = stop, rotation = continue)
- Don't use cumulative time (just per-iteration)

::notes
Time limit gutter: >1 hour per iteration means stuck. Different from rotation (>30 min rotates, >1 hour = gutter). Safety net for truly stuck agents.
