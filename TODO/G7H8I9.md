---
id: G7H8I9
status: pending
deps: [D4E5F6]
files: [scripts/milhouse.sh]
---
::context
Check if all task criteria are complete by counting [x] vs [ ] checkboxes. This determines if loop should stop.

::done-when
- Function check_completion() returns "COMPLETE" or "INCOMPLETE:N"
- Returns "COMPLETE" when all [ ] become [x]
- Returns "INCOMPLETE:N" with remaining count when some [ ] remain
- Handles edge case: task file with no checkboxes

::steps
1. Use read_task_file() to get checkbox counts
2. If completed == total and total > 0: return "COMPLETE"
3. Else return "INCOMPLETE:{remaining_count}"
4. Add edge case: no checkboxes = "NO_TASK"

::avoid
- Don't modify task file (agent does that)
- Don't parse task content beyond checkboxes
- Keep it simple - just count, don't analyze

::notes
This is how we detect completion. Agent marks [ ] as [x] when done. We just verify all are checked. Reference ralph-common.sh check_task_complete() function.
