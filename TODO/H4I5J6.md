---
id: H4I5J6
status: pending
deps: [A1B2C3]
files: [scripts/milhouse.sh]
---
::context
Add prerequisite checks: verify git repo exists, cursor-agent is installed, MILHOUSE_TASK.md exists. Check before starting any operations.

::done-when
- Function check_prerequisites(workspace) validates all requirements
- Checks: git repo exists (git rev-parse --git-dir)
- Checks: cursor-agent exists (command -v cursor-agent)
- Checks: MILHOUSE_TASK.md exists
- Returns 0 if all good, 1 if any missing, with helpful error messages

::steps
1. Create check_prerequisites(workspace) function
2. Check git: git -C "$workspace" rev-parse --git-dir > /dev/null 2>&1
3. Check cursor-agent: command -v cursor-agent &> /dev/null
4. Check task file: test -f "$workspace/MILHOUSE_TASK.md"
5. Print helpful error messages for each missing prerequisite
6. Return 1 if any fail, 0 if all pass
7. Call this early in main(), before any operations

::avoid
- Don't check gum (optional dependency)
- Don't check permissions (let them fail naturally)
- Keep checks simple and fast

::notes
These are hard requirements. Without git, cursor-agent, or task file, Milhouse can't work. Check early and fail fast with clear messages. Reference ralph-common.sh check_prerequisites().
