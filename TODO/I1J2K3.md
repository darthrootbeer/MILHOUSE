---
id: I1J2K3
status: pending
deps: [S9T0U1]
files: [scripts/milhouse.sh]
---
::context
Implement agent signal detection: grep agent output for <milhouse>GUTTER</milhouse> signal, stop loop if detected.

::done-when
- Function check_agent_gutter_signal(output_file) greps for gutter signal
- Looks for pattern: <milhouse>GUTTER</milhouse> in agent output
- Returns true if signal found
- Integration: check output after iteration, stop if signal

::steps
1. After agent completes, read output file (.milhouse/out.txt)
2. Grep for pattern: <milhouse>GUTTER</milhouse>
3. If found: return GUTTER
4. Add to main loop: check output, stop if signal
5. Log: "Agent signaled gutter - stopping loop"

::avoid
- Don't parse entire output (just grep for signal)
- Don't check during iteration (only after completion)

::notes
Agent self-reporting: if agent outputs gutter signal, it knows it's stuck. Simple grep check. Agent includes this in prompt instructions (from build_prompt).
