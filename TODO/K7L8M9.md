---
id: K7L8M9
status: pending
deps: [Y5Z6A7]
files: [scripts/milhouse.sh]
---
::context
Implement time-based rotation: measure iteration duration and rotate if >30 minutes. Track start/end time, calculate duration.

::done-when
- Function should_rotate_time(duration_seconds) returns true if >1800 (30 min)
- Track iteration start time before agent runs
- Track iteration end time after agent completes
- Calculate duration and check threshold
- Integration: check in main loop after iteration

::steps
1. Add time tracking: START_TIME=$(date +%s) before agent run
2. Add time tracking: END_TIME=$(date +%s) after agent completes
3. Calculate: DURATION=$((END_TIME - START_TIME))
4. Add should_rotate_time(duration): return true if duration > 1800
5. Add to main loop: check after iteration, rotate if true

::avoid
- Don't track cumulative time (just per-iteration)
- Don't use complex time parsing (just unix timestamps)
- Keep threshold configurable but default 30 min

::notes
Time-based rotation: if an iteration takes >30 minutes, assume context is filling up. Simple heuristic. 30 minutes = 1800 seconds. Can be made configurable later.
