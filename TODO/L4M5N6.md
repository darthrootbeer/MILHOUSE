---
id: L4M5N6
status: pending
deps: [Z2A3B4, C5D6E7, F8G9H0, I1J2K3]
files: [scripts/milhouse.sh]
---
::context
Integrate all gutter detection checks: combine git log, task file stale, time limit, agent signal. Stop loop if ANY gutter detected.

::done-when
- Function check_gutter() combines all gutter checks with OR logic
- Checks: git log errors, task file stale, time limit, agent signal
- Returns GUTTER or OK
- Integration: check after iteration, stop loop if gutter
- Logs which gutter check triggered

::steps
1. Create check_gutter() function that combines all checks
2. Call: check_git_log_errors(), check_task_file_stale(), check_time_limit(), check_agent_gutter_signal()
3. OR logic: if ANY return GUTTER, check_gutter() returns GUTTER
4. Track which check triggered (for logging)
5. Add to main loop: check after iteration, if GUTTER stop and report
6. Log: "🚨 Gutter detected: {reason}"

::avoid
- Don't use AND logic (ANY gutter = stop)
- Don't continue after gutter (must stop)

::notes
Combines all gutter detection. Any trigger = stop loop. This is Phase 3 completion - detect when agent is stuck and stop gracefully. Reference MILHOUSE_PLAN.md.
