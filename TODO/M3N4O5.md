---
id: M3N4O5
status: pending
deps: [A1B2C3]
files: [scripts/milhouse.sh]
---
::context
Create .milhouse/ directory structure and initialize state files if they don't exist. State files: iteration counter, progress.md, guardrails.md

::done-when
- Function init_state(workspace) creates .milhouse/ if missing
- Creates .milhouse/iteration file with "0" if missing
- Creates .milhouse/progress.md with template if missing
- Creates .milhouse/guardrails.md with template if missing
- Never overwrites existing files

::steps
1. Add init_state(workspace) function
2. mkdir -p .milhouse/
3. If .milhouse/iteration missing: echo "0" > .milhouse/iteration
4. If .milhouse/progress.md missing: write template with header
5. If .milhouse/guardrails.md missing: write template with core signs

::avoid
- Don't overwrite existing state files (preserve history)
- Don't create activity/error logs (use git instead)
- Keep templates minimal - agent will fill them

::notes
State files persist across iterations. Agent reads/writes progress.md and guardrails.md. We only manage iteration counter. Reference ralph-common.sh init_ralph_dir() for templates but simplify.
