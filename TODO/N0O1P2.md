---
id: N0O1P2
status: pending
deps: [Y5Z6A7]
files: [scripts/milhouse.sh]
---
::context
Implement file count-based rotation: count files changed in iteration via git diff, rotate if >50 files touched.

::done-when
- Function count_files_changed() returns number of files changed in iteration
- Uses git diff --stat to count files
- Function should_rotate_files(file_count) returns true if >50
- Integration: check in main loop after iteration

::steps
1. Before iteration: record HEAD commit hash or use git diff base
2. After iteration: count changed files: git diff --stat | wc -l (or better: git diff --name-only | wc -l)
3. Add should_rotate_files(count): return true if count > 50
4. Handle edge case: no changes (count = 0, no rotation)
5. Add to main loop: check after iteration, rotate if true

::avoid
- Don't count untracked files (just git-tracked changes)
- Don't parse diff content (just count files)
- Keep threshold configurable but default 50

::notes
File-based rotation: if >50 files changed in one iteration, rotate. Indicates agent is touching many files, context likely filling. Uses git diff to count. Reference MILHOUSE_PLAN.md for heuristic.
