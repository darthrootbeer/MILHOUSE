---
id: O7P8Q9
status: pending
deps: [A1B2C3]
files: [scripts/milhouse.sh]
---
::context
Add gum detection: check if gum is installed, set HAS_GUM flag. All gum UI code should check this flag and fallback to plain prompts if false.

::done-when
- Function check_gum() returns true if gum installed, false otherwise
- Sets HAS_GUM=true/false variable
- Called early in script initialization
- All gum commands wrapped in if [ "$HAS_GUM" == "true" ] checks

::steps
1. Add check_gum() function: command -v gum &> /dev/null
2. Set HAS_GUM variable based on check
3. Call check_gum() early in main() or script init
4. Document: gum is optional, script works without it

::avoid
- Don't require gum (optional dependency)
- Don't fail if gum missing (just use plain prompts)

::notes
Gum is optional. Script must work without it using plain read/echo. Check once at start, set flag, use throughout. Reference ralph-setup.sh HAS_GUM pattern.
