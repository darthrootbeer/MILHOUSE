---
id: P4Q5R6
status: pending
deps: [T6U7V8, W9X0Y1, M1N2O3]
files: []
---
::context
Test Phase 2: verify rotation heuristics work correctly. Test time-based, file-based, commit-based, and fixed rotation triggers.

::done-when
- Time-based rotation triggers when iteration >30 min (or simulate)
- File-based rotation triggers when >50 files changed (or simulate)
- Commit-based rotation triggers when >10 commits (or simulate)
- Fixed rotation still works (every 5 iterations)
- Rotation reporting shows correct reason

::steps
1. Test fixed rotation: run 6 iterations, verify rotation at iteration 5
2. Simulate time-based: set threshold low (1 min), run long iteration
3. Simulate file-based: create >50 files, verify rotation
4. Simulate commit-based: make >10 commits in one iteration
5. Verify rotation reporting shows correct reason
6. Verify loop continues after rotation (doesn't stop)

::avoid
- Don't break Phase 1 functionality
- Don't test Phase 3/4 yet

::notes
Phase 2 testing verifies smart rotation heuristics work. May need to simulate conditions (lower thresholds for testing). Ensure rotation works but doesn't stop loop (different from gutter).
