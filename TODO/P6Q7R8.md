---
id: P6Q7R8
status: pending
deps: [M3N4O5]
files: [scripts/milhouse.sh]
---
::context
Read and write iteration counter from .milhouse/iteration file. Track current iteration number for rotation logic.

::done-when
- Function get_iteration(workspace) returns current iteration number (default 0)
- Function set_iteration(workspace, num) writes iteration number
- Function increment_iteration(workspace) increments and returns new value
- All functions handle missing file gracefully

::steps
1. Add get_iteration(workspace): cat .milhouse/iteration or echo "0" if missing
2. Add set_iteration(workspace, num): echo "$num" > .milhouse/iteration
3. Add increment_iteration(workspace): read current, add 1, write back, return new
4. Test with missing file (should default to 0)

::avoid
- Don't add complex state management
- Don't store anything else in iteration file (just number)
- Keep functions simple - just read/write a number

::notes
Iteration counter is simple: just a number. We increment it each iteration. Used for fixed rotation (every N iterations) and tracking progress. Reference ralph-common.sh get_iteration(), set_iteration().
