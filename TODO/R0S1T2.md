---
id: R0S1T2
status: pending
deps: [O7P8Q9]
files: [scripts/milhouse.sh]
---
::context
Implement interactive setup with gum: model selection, options (branch, PR), confirmation. Follows GUM_TUI_RULES.md for best practices. Falls back to plain prompts if gum not available.

::done-when
- Function interactive_setup() shows gum UI for configuration
- Model selection: gum choose with list of models
- Options: gum choose --no-limit for multi-select (branch, PR, etc.)
- Confirmation: gum confirm before starting loop
- Fallback: plain read/echo if HAS_GUM=false
- Sets MODEL, MAX_ITERATIONS, USE_BRANCH, OPEN_PR variables

::steps
1. Create interactive_setup() function
2. If HAS_GUM: use gum choose for model selection
3. If HAS_GUM: use gum choose --no-limit for options
4. If HAS_GUM: use gum confirm for confirmation
5. Else: use read/echo for plain prompts
6. Set all configuration variables
7. Return configuration for use in loop

::avoid
- Don't require gum (always provide fallback)
- Don't skip confirmation (always ask before starting)

::notes
Interactive setup makes configuration easy. Follow GUM_TUI_RULES.md: consistent styling, clear prompts, graceful degradation. Reference ralph-setup.sh but simplify.
