---
id: S9T0U1
status: pending
deps: [J0K1L2]
files: [scripts/milhouse.sh]
---
::context
Execute cursor-agent with the built prompt and capture output. This is the core operation - running the agent for one iteration.

::done-when
- Function run_agent_iteration(workspace, prompt) executes cursor-agent
- Captures output to .milhouse/out.txt (or similar)
- Waits for agent to complete
- Returns exit code or signal
- Handles missing cursor-agent gracefully

::steps
1. Add run_agent_iteration(workspace, prompt, model) function
2. Build command: cursor-agent -p --force --model "$model" "$prompt"
3. Redirect output: > .milhouse/out.txt 2>&1 (or capture for analysis)
4. Execute and wait: wait for process to finish
5. Check exit code and return
6. Add prerequisite check: verify cursor-agent exists before running

::avoid
- Don't parse output in real-time (post-iteration analysis in Phase 2)
- Don't add rotation logic yet (just run once)
- Don't stream parse (save output for later analysis)

::notes
This is the core - run agent, wait for completion. Output can be analyzed after (Phase 2). Reference ralph-loop.sh but simplify - no stream parsing, just run and wait. Model parameter comes from --model flag or default.
