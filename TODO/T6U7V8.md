---
id: T6U7V8
status: pending
deps: [K7L8M9, N0O1P2, Q3R4S5]
files: [scripts/milhouse.sh]
---
::context
Integrate all rotation heuristics with OR logic: rotate if time >30min OR files >50 OR commits >10 OR fixed rotation. Combine with fixed rotation from Phase 1.

::done-when
- Function should_rotate() combines all heuristics with OR logic
- Checks: time-based, file-based, commit-based, fixed rotation
- Returns true if ANY condition met
- Integration: single check in main loop after iteration
- Logs which heuristic triggered rotation

::steps
1. Create should_rotate() function that combines all checks
2. Call: should_rotate_time(), should_rotate_files(), should_rotate_commits(), should_rotate_fixed()
3. OR logic: if ANY return true, should_rotate() returns true
4. Track which heuristic triggered (for logging)
5. Replace fixed rotation check in main loop with should_rotate()
6. Log rotation reason: "Rotating due to: time/files/commits/fixed"

::avoid
- Don't use AND logic (rotate if ANY condition)
- Don't remove fixed rotation (combine with heuristics)
- Keep all heuristics independent (OR, not weighted)

::notes
Combines all rotation heuristics. Any trigger = rotate. Log which one triggered for debugging. This is Phase 2 completion - smart rotation instead of just fixed.
