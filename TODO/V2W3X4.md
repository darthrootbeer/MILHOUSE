---
id: V2W3X4
status: pending
deps: [P6Q7R8]
files: [scripts/milhouse.sh]
---
::context
Implement fixed rotation logic: rotate (start fresh context) every N iterations. Default N=5. Rotation means incrementing iteration and starting new agent session.

::done-when
- Function should_rotate_fixed(iteration, rotate_interval) returns true/false
- Returns true when iteration % rotate_interval == 0 (but not iteration 0)
- Default rotate_interval is 5
- Integration: check after each iteration, rotate if true

::steps
1. Add should_rotate_fixed(iteration, interval) function
2. Logic: if iteration > 0 and iteration % interval == 0, return true
3. Default interval: 5 iterations
4. Add to main loop: check after iteration completes, rotate if needed
5. Rotation action: log message, increment iteration counter (already handled)

::avoid
- Don't add heuristic rotation yet (Phase 2)
- Don't add gutter detection yet (Phase 3)
- Keep it simple - just modulo check

::notes
Fixed rotation is simplest: every N iterations, force a rotation. This ensures fresh context periodically. Phase 2 will add smarter heuristics. For now, this prevents infinite context buildup.
