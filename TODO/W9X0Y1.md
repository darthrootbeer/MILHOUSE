---
id: W9X0Y1
status: pending
deps: [T6U7V8]
files: [scripts/milhouse.sh]
---
::context
Add rotation reporting: log why rotation occurred (time/files/commits/fixed) so user can understand and tune thresholds.

::done-when
- Log rotation with reason when should_rotate() returns true
- Show which heuristic triggered: "Rotating: time threshold (35 min > 30 min)"
- Include relevant metrics: duration, file count, commit count, iteration number
- Output is clear and actionable

::steps
1. Modify should_rotate() to return reason string, not just true/false
2. Or: track which check passed and log accordingly
3. Log format: "🔄 Rotation triggered: {reason} ({metric} > {threshold})"
4. Examples: "time (35 min)", "files (52 files)", "commits (12 commits)", "fixed (iteration 5)"

::avoid
- Don't log too verbosely (one line per rotation)
- Don't require complex formatting (simple is fine)

::notes
User needs to know why rotation happened to tune thresholds. Clear logging helps debug and optimize. Keep it one line per rotation with key metric.
