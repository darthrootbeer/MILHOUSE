---
id: X6Y7Z8
status: done
deps: [Y5Z6A7, O7P8Q9]
files: [scripts/milhouse.sh]
---
::context
Add progress reporting: show iteration number, criteria progress (X/Y complete), status indicators. Use gum style for formatting when available.

::done-when
- Function show_progress(iteration, done, total) displays progress
- Shows: "Iteration N: X/Y criteria complete (Z remaining)"
- Updates between iterations
- Uses gum style for formatting if available
- Clear and readable format

::steps
1. Create show_progress(iteration, done, total, remaining) function
2. Format: "Iteration {iteration}: {done}/{total} complete ({remaining} remaining)"
3. If HAS_GUM: use gum style --border single --padding "0 1"
4. Else: use plain echo with clear formatting
5. Call after each iteration completion check
6. Update before showing next iteration start

::avoid
- Don't overwrite too frequently (once per iteration is enough)
- Don't make it too verbose (one line is good)

::notes
Progress reporting helps users track advancement. Show clear metrics: iteration number and criteria progress. Use gum for polish but keep it simple. Reference activity logs but simpler.
