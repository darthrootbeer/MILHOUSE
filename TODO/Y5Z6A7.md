---
id: Y5Z6A7
status: pending
deps: [G7H8I9, S9T0U1, V2W3X4, P6Q7R8]
files: [scripts/milhouse.sh]
---
::context
Implement main loop that orchestrates iterations: run agent, check completion, check rotation, continue or stop. This ties all Phase 1 components together.

::done-when
- Main loop function run_milhouse_loop(workspace, max_iterations) works
- Loop runs iterations until: completion, max iterations, or error
- Each iteration: build prompt, run agent, check completion, check rotation
- Logs progress between iterations
- Returns 0 on success, 1 on failure/max iterations

::steps
1. Create run_milhouse_loop(workspace, max_iterations, model) function
2. Initialize: get_iteration(), check_completion() first
3. While iteration <= max_iterations and not complete:
   - Build prompt with build_prompt(iteration)
   - Run agent with run_agent_iteration()
   - Check completion with check_completion()
   - If complete: return 0 (success)
   - Check should_rotate_fixed() - if true, log rotation
   - Increment iteration with increment_iteration()
   - Brief pause between iterations (sleep 2)
4. If max iterations reached: return 1
5. Log summary at end

::avoid
- Don't add heuristic rotation checks yet (Phase 2)
- Don't add gutter detection yet (Phase 3)
- Don't add gum UI yet (Phase 4)
- Keep error handling basic for now

::notes
This is the orchestrator - it uses all the Phase 1 functions. Should be clean and readable. This is what gets called from --loop mode. Reference ralph-common.sh run_ralph_loop() but simplify.
