---
id: Z2A3B4
status: pending
deps: [Y5Z6A7]
files: [scripts/milhouse.sh]
---
::context
Implement git log error pattern detection: scan last 20 commits for repeated error messages, detect gutter if same error appears 3+ times.

::done-when
- Function check_git_log_errors() scans recent commits for error patterns
- Detects repeated error messages in commit messages or git log
- Returns true if same error appears 3+ times
- Integration: check in main loop, stop if gutter detected

::steps
1. Get last 20 commits: git log --oneline -20
2. Parse commit messages for error patterns (look for "error", "fail", etc.)
3. Count occurrences of each unique error pattern
4. If any pattern appears 3+ times: return GUTTER
5. Add check to main loop: if gutter detected, stop and report

::avoid
- Don't parse commit diffs (just messages)
- Don't check entire git history (just recent commits)
- Keep pattern matching simple (grep for common error words)

::notes
Gutter detection via git log: if same error repeated 3x in recent commits, agent is stuck. Simple heuristic using git log --oneline. Reference MILHOUSE_PLAN.md.
